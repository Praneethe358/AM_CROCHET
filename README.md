the home page needed to call the Render backend, not Vercel’s /api/home
the hero data was already correct once the backend URL was used
the promotions section depended on active records with the exact placement/status/date filters